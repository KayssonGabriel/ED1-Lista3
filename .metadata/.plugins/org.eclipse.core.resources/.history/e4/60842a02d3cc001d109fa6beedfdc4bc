package algoritmo481;

public class Calc {

	int converte(int nNum, int nBase) {
		int b = 0;
		int nb = 0;

		while (nNum >= nBase) {
			int r = nNum % nBase;
			nb += Math.pow(10, b) * r;
			b++;
		}
		nb += Math.pow(10, b) * nNum;
		return (nb);

	}

}
